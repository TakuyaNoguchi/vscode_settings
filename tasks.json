{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CheckTestCase",
            "type": "shell",
            "command": "oj t -c \"pypy ${fileDirname}/${fileBasename}\" -d ${fileDirname}/test/"
            "presentation": {
                "reveal": "always",
                "focus": true,
                "panel": "shared",
            }
        }
    ]
}
